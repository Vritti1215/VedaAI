"use client";
import {FormEvent,useState} from "react";import {useRouter} from "next/navigation";import Link from "next/link";import {Sparkles,Loader2} from "lucide-react";
export default function Login(){const r=useRouter();const [email,setEmail]=useState("");const[p,setP]=useState("");const[e,setE]=useState("");const[busy,setBusy]=useState(false);async function submit(x:FormEvent){x.preventDefault();setBusy(true);setE("");try{const q=await fetch("/api/auth/login",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password:p})});const d=await q.json();if(!q.ok)throw Error(d.error);r.push("/dashboard");}catch(x){setE(x instanceof Error?x.message:"Unable to sign in");}finally{setBusy(false)}}return <AuthShell title="Welcome back" subtitle="Sign in to continue analysing assessments"><form onSubmit={submit}><label>Email<input type="email" value={email} onChange={x=>setEmail(x.target.value)} required placeholder="teacher@example.com"/></label><label>Password<input type="password" value={p} onChange={x=>setP(x.target.value)} required placeholder="••••••••"/></label>{e&&<div className="error">{e}</div>}<button className="authBtn" disabled={busy}>{busy?<Loader2 className="spin" size={16}/>:"Sign in"}</button></form><p className="authFoot">Don't have an account? <Link href="/register">Create one</Link></p></AuthShell>}
function AuthShell({title,subtitle,children}:{title:string;subtitle:string;children:React.ReactNode}){return <main className="authPage"><div className="authBrand"><div className="logoMark">V</div><b>VedaAI</b></div><section className="authCard"><div className="sparkAuth"><Sparkles size={18}/></div><h1>{title}</h1><p>{subtitle}</p>{children}</section></main>}
